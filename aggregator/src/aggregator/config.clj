(ns aggregator.config)

(def whitelist
  #{"hhu.de"
    "schneider.gg"
    "dbas"})

(def aggregate-name
  "hhu.de") 

;; -----------------------------------------------------------------------------
;; Broker-Configuration

(def subscribe-to
  #{"broker"})

(def blacklist
  #{"evil.com"})
